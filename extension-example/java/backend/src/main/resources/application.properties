# Extension opens NO listening port; Core fronts all HTTP via the gRPC tunnel.
# Disable the embedded servlet container's network listener.
server.port=-1
spring.main.web-application-type=servlet
spring.application.name=java_example
